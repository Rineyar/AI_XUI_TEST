#FROM python:3.14-slim-trixie
FROM python:3.12-slim

#Имя
LABEL description="Agent runtime test container"

#Переменные среды
ENV PYTHONUNBUFFERED=1
ENV RUST_BACKTRACE=1

#Рабочая область
WORKDIR /agent/agent_core

#Взять собранный под контейнер ELF
COPY agent/agent_core/target/x86_64-unknown-linux-gnu/release/agent_core /agent/agent_core

# #Инструменты агента Теперь собраны внутри агента
# COPY agent/tools/tools_c  /agent/tools/tools_c
# COPY agent/tools/tools_py /agent/tools/tools_py
# COPY agent/tools/tools_rs /agent/tools/tools_rs

#Добавить необходимые библиотеки питону
RUN pip install --no-cache-dir requests

#Гвард
COPY agent/guards.py /agent/guards.py

#Создание пользователя, выдача прав
RUN chmod +x /agent/agent_core/agent_core && useradd --create-home --uid 1000 agent && chown -R agent:agent /agent

#На будущее сделать agent только read

#Дальше пашешь ты
USER agent

#Запуск
ENTRYPOINT ["./agent_core", "-D"]